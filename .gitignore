# Xcode / build (regenerate with: xcodegen generate)
*.xcodeproj
build/
DerivedData/
*.xcuserstate
xcuserdata/

# Local signing (never commit)
Config/Signing.xcconfig

# macOS
.DS_Store

# Data leads pending human review — never auto-published
Tools/leads.json

# Ingestion engine run-state (which pages have been checked, and when) —
# local cache, not source data, and machine-specific until a CI cache exists
Tools/ingest/.state/
